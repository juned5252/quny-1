var gulp = require('gulp');
var del = require('del');
var vp = require('vinyl-paths');
var paths = require('../paths');

/**
 * Deletes /builtAssets
 * These files are generated by connect-assets when NODE_ENV=production
 */
gulp.task('clean:assets', function () {
  return gulp.src(paths.assets)
    .pipe(vp(del));
});

/** Deletes /bin */
gulp.task('clean:bin', [
  'clean:coverage',
  'clean:drivers',
  'clean:reports'
]);
